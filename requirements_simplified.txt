# Core Flask dependencies
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-JWT-Extended==4.5.3
Flask-CORS==4.0.0
Flask-Migrate==4.0.5
Werkzeug==2.3.7
psycopg2-binary==2.9.7
requests==2.31.0
python-dotenv==1.0.0
bcrypt==4.0.1
email-validator==2.0.0
gunicorn==21.2.0

# Basic web scraping
beautifulsoup4==4.12.2
lxml==4.9.3
selenium==4.15.0
webdriver-manager==4.0.1

# Image processing
Pillow==10.0.0

# Basic text analysis (without spacy for now)
nltk==3.8.1
textblob==0.17.1

# Remove problematic packages for now:
# sherlock-project==0.14.1  # Comment out - causes metadata issues
# spacy==3.6.1              # Comment out - requires system dependencies
# telethon==1.28.5          # Comment out - complex dependencies
# instagrapi==1.16.22       # Comment out - heavy dependencies
