# Minimal dependencies for scraping functionality
Flask==2.3.3
Flask-SQLAlchemy==3.0.5
Flask-CORS==4.0.0
python-dotenv==1.0.0
requests==2.31.0
bcrypt==4.0.1
Werkzeug==2.3.7

# Essential scraping tools (install separately if needed)
# telethon  # For Telegram - install separately: pip install telethon
# instagrapi  # For Instagram - install separately: pip install instagrapi
